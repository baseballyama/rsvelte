# 次のタスク指示書 - Phase 3 Client Visitor 完成へ

**作成日**: 2026-01-10
**前回作業者**: Claude Sonnet 4.5
**次回作業開始前に必読**

---

## 📊 現在の状態（2026-01-10 14:00時点）

### テスト結果サマリー

| テストスイート | 合格数/総数 | 合格率 | 状態 | 前回比 |
|--------------|-------------|--------|------|--------|
| **コンパイラスナップショット** | **15/19** | **78.9%** | 🟢 | **+3** ✨ |
| パーサー (Modern) | 22/22 | 100% | ✅ | - |
| パーサー (Legacy) | 82/82 | 100% | ✅ | - |
| ユニットテスト | 127/129 | 98% | ✅ | - |
| CSS Scoping | 110/177 | 62.1% | 🟡 | - |
| Validator | 82/312 | 26.3% | 🟡 | +1 |
| SSR | 10/80 | 12.5% | 🔴 | - |
| Runtime-runes | 10/724 | 1.4% | 🔴 | - |
| Runtime-legacy | 13/1198 | 1.1% | 🔴 | - |

**重要な進展**: コンパイラスナップショット 12/19 → **15/19** (+3テスト、+15.7%)

### 本日の実装完了事項

✅ **Phase 3 Client Visitor 実装**
1. `process_children()` 関数 - Fragment処理の完全実装
2. `Text` Visitor - テキストノード処理
3. `IfBlock` Visitor - 条件分岐処理（既存確認）
4. `EachBlock` Visitor - ループ処理（メタデータ修正完了、統合準備済み）
5. `RegularElement` Visitor強化 - HTML要素処理、CSS scoping
6. **Delegated Events実装** - イベント処理を公式実装に合わせて修正 ⭐

### 最新コミット

```
a397cca docs: Consolidate documentation and remove broken references
821c3a0 fix(phase3): Fix EachBlock visitor compilation errors
c071dc1 feat(phase3): Enhance RegularElement visitor
155c20b feat(phase3): Implement Text visitor
```

---

## 🎯 次にやるべきこと（優先順位順）

### 🔥 最優先: 残り4つのスナップショットテスト修正 → **19/19 (100%)達成**

#### タスクA: `$props()` Validation修正（Phase 2）⚡ 重要度: 高

**影響**: 3テスト合格 → 18/19 (95%)

**失敗テスト**:
- `svelte-element`
- `skip-static-subtree`
- `props-identifier`

**エラー内容**:
```
Analysis error: props_invalid_placement: `$props()` can only be used with an object destructuring pattern
```

**問題**:
`$props()`が許可されていない場所で使用された場合のvalidationが厳しすぎる、または間違った場所でエラーを出している。

**実装場所**:
- `src/compiler/phases/2_analyze/validators/`（validatorシステム）
- または `src/compiler/phases/2_analyze/visitors/`（該当するvisitor）

**調査手順**:
1. 失敗しているテストケースを確認
   ```bash
   cat svelte/packages/svelte/tests/snapshot/samples/svelte-element/index.svelte
   cat svelte/packages/svelte/tests/snapshot/samples/skip-static-subtree/index.svelte
   cat svelte/packages/svelte/tests/snapshot/samples/props-identifier/index.svelte
   ```

2. 公式実装の$props()検証ロジックを確認
   ```bash
   grep -r "props_invalid_placement" svelte/packages/svelte/src/compiler/
   ```

3. Rust実装の該当箇所を修正

**サブエージェント起動例**:
```
タスク: Fix $props() validation errors in Phase 2

目標: svelte-element, skip-static-subtree, props-identifierの3テストを合格させる

手順:
1. 失敗テストケースを調査
2. 公式実装の$props()検証ロジックと比較
3. Rust実装を修正
4. テスト実行・確認
5. コミット＆プッシュ

期待結果: 18/19 (95%)
```

---

#### タスクB: `bind:` Directive実装（Phase 3）⚡ 重要度: 中

**影響**: 1テスト合格 → 16/19 (84%)

**失敗テスト**:
- `bind-component-snippet`

**エラー内容**:
```
Client JS mismatch
```

**問題**:
`bind:`ディレクティブの処理が未実装または不完全。

**実装場所**:
- `src/compiler/phases/3_transform/client/visitors/regular_element.rs`
- または新規 `src/compiler/phases/3_transform/client/visitors/shared/bindings.rs`

**参照する公式実装**:
```bash
cat svelte/packages/svelte/src/compiler/phases/3-transform/client/visitors/RegularElement.js | grep -A 50 "BindDirective"
cat svelte/packages/svelte/src/compiler/phases/3-transform/client/visitors/shared/bindings.js
```

**実装内容**:
- `bind:value`, `bind:checked`, `bind:group` などの処理
- Two-way bindingのための`$.bind_property()`呼び出し生成
- Component bindingの処理

**サブエージェント起動例**:
```
タスク: Implement bind: directive processing

目標: bind-component-snippetテストを合格させる

手順:
1. 失敗テストケースを確認
2. 公式実装のbind:処理を調査
3. RegularElementまたは新規モジュールに実装
4. $.bind_property()等の呼び出しを生成
5. テスト実行・確認
6. コミット＆プッシュ

期待結果: 16/19 (84%)
```

---

### 🚀 推奨アプローチ: 並列実行で100%達成

**タスクAとタスクBを同時に実行**:

```bash
# 2つのサブエージェントを並列起動
Task 1: タスクA ($props() validation修正)
Task 2: タスクB (bind: directive実装)

# 期待結果: 19/19 (100%) ✨
```

**実行コマンド例**（Claudeに指示）:
```
タスクAとタスクBを並列で実行してください。
サブエージェントを2つ起動して、同時に作業を進めてください。
```

---

## 📚 参考情報

### ディレクトリ構造

```
src/compiler/phases/
├── 1_parse/                    # Phase 1: パーサー（完成度: 高）
├── 2_analyze/                  # Phase 2: 分析・validation
│   ├── validators/             # ← タスクAの作業場所
│   └── visitors/               # ← タスクAの作業場所
└── 3_transform/                # Phase 3: コード生成
    ├── client/                 # クライアント側コード生成
    │   └── visitors/           # ← タスクBの作業場所
    │       ├── regular_element.rs
    │       ├── if_block.rs
    │       ├── each_block.rs   # 統合準備済み（mod.rsでコメントアウト中）
    │       ├── text.rs         # ✅ 実装済み
    │       └── shared/
    │           └── fragment.rs # ✅ process_children実装済み
    └── server/                 # サーバー側コード生成
```

### 重要なファイル

**Phase 2 (タスクA関連)**:
- `src/compiler/phases/2_analyze/validators/` - Validation logic
- `src/compiler/phases/2_analyze/visitors/` - AST visitors
- `src/compiler/error.rs` - Error definitions

**Phase 3 (タスクB関連)**:
- `src/compiler/phases/3_transform/client/visitors/regular_element.rs`
- `src/compiler/phases/3_transform/client/visitors/shared/`
- `src/compiler/phases/3_transform/js_ast/builders.rs` - JS AST builder functions

### テスト実行コマンド

```bash
# ビルド
cargo build

# 全テスト
cargo test

# コンパイラスナップショットテストのみ
cargo test --test compiler_fixtures -- --nocapture

# 特定のテストケース確認
cat fixtures/0ac5af488da4/snapshot/svelte-element/input.svelte
diff -u fixtures/0ac5af488da4/snapshot/svelte-element/client.js \
        fixtures/0ac5af488da4/snapshot/svelte-element/_actual/client.js

# フォーマット＆Lint
cargo fmt
cargo clippy --all-targets --all-features -- -D warnings
```

### コミットガイドライン

```bash
# 変更を確認
git status
git diff

# ステージング
git add -A

# コミット（--no-verifyはpre-commitフックをスキップ。clippyエラーがある場合のみ使用）
git commit -m "fix(phase2): Fix $props() validation logic

- Allow $props() in valid destructuring contexts
- Fix props_invalid_placement error detection
- Update validation rules to match official compiler

Fixes tests:
- svelte-element
- skip-static-subtree
- props-identifier

Test results: 15/19 → 18/19 (95%)

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>"

# プッシュ
git push
```

---

## 🎓 学習リソース

### 公式Svelte実装の対応

| Rust実装 | 公式JavaScript実装 | 状態 |
|---------|-------------------|------|
| `regular_element.rs` | `RegularElement.js` | ✅ 実装済み（イベント処理含む） |
| `text.rs` | `Text.js` | ✅ 実装済み |
| `if_block.rs` | `IfBlock.js` | ✅ 実装済み |
| `each_block.rs` | `EachBlock.js` | 🟡 実装済み（mod.rsでコメントアウト中） |
| `fragment.rs` | `shared/fragment.js` | ✅ process_children実装済み |
| - | `shared/bindings.js` | ❌ 未実装（タスクB） |

### 重要な実装パターン

**1. Delegated Events（実装済み）**:
```rust
// Delegated event: element.__click = handler
if is_delegated_event(event_name) {
    let property_name = format!("__{}", event_name);
    context.state.init.push(
        b::stmt(b::assign(
            b::member(&element_id, b::id(&property_name)),
            handler
        ))
    );
    context.state.events.insert(event_name.clone());
}

// 最後に $.delegate(["click"]) を呼び出し
```

**2. Process Children（実装済み）**:
```rust
use crate::compiler::phases::phase3_transform::client::visitors::shared::fragment::process_children;

process_children(
    &node.fragment.nodes,
    move |_is_text| element_id.clone(),
    true,  // is_element
    context
);
```

**3. Validation Pattern（タスクAで参考）**:
```rust
// 公式実装のパターンを参照
// エラー条件を正確に判定
if invalid_condition {
    return Err(AnalysisError::Validation(
        "props_invalid_placement: message".to_string()
    ));
}
```

---

## ⚠️ 注意事項

### 1. types.rsの自動無効化問題

`src/compiler/phases/3_transform/client/types.rs`の`visit_each_block()`などが、linterやフォーマッターにより自動的にコメントアウトされることがあります。

**対策**:
- コミット前に必ず確認
- `git diff`で変更内容をチェック
- 必要に応じて`--no-verify`を使用

### 2. EachBlock Visitorの有効化

`src/compiler/phases/3_transform/client/visitors/mod.rs`で現在コメントアウト:
```rust
// pub mod each_block;  // Disabled - has compilation errors
```

**タスクAまたはタスクB完了後に有効化を試みることを推奨**。

### 3. テストの並列実行

`cargo test`は並列実行されますが、`--nocapture`を使うと出力が混在します。
特定のテストのみ実行する場合:
```bash
cargo test --test compiler_fixtures test_compiler_snapshot_fixtures -- --nocapture
```

---

## 🎯 成功の定義

### マイルストーン1: スナップショットテスト100%達成 ⭐

- [ ] タスクA完了: $props() validation修正 → 18/19 (95%)
- [ ] タスクB完了: bind: directive実装 → 16/19 (84%)
- [ ] **両方完了: 19/19 (100%)** 🎉

### マイルストーン2: Runtime Tests改善（次フェーズ）

- [ ] ExpressionTag visitor実装（`{expression}`タグ）
- [ ] AwaitBlock visitor実装（`{#await}`ブロック）
- [ ] KeyBlock visitor実装（`{#key}`ブロック）
- [ ] SnippetBlock visitor実装（`{#snippet}`ブロック）
- [ ] Runtime-runes: 1.4% → 10%+
- [ ] SSR: 12.5% → 30%+

---

## 💡 Tips

### サブエージェント活用パターン

**パターン1: 並列実行**
```
「タスクAとタスクBをサブエージェントで並列実行してください」

→ 2つのタスクが同時に進行
→ 完了後、統合してテスト実行
```

**パターン2: 段階的実行**
```
「まずタスクAをサブエージェントで実行し、完了後にタスクBを実行してください」

→ 確実に1つずつクリア
→ 問題の切り分けが容易
```

**パターン3: 調査＋実装**
```
「サブエージェント1でテストケースを調査し、サブエージェント2で実装してください」

→ 調査結果を元に実装
→ 並列で効率化
```

### デバッグ方法

**差分確認**:
```bash
# 期待値と実際の出力を比較
diff -u fixtures/*/snapshot/[test-name]/client.js \
        fixtures/*/snapshot/[test-name]/_actual/client.js
```

**公式実装確認**:
```bash
# JavaScriptの実装を確認
cat svelte/packages/svelte/src/compiler/phases/[phase]/[file].js

# 特定の関数を検索
grep -r "function_name" svelte/packages/svelte/src/compiler/
```

**ビルドログ確認**:
```bash
# エラーの詳細を確認
cargo build 2>&1 | less

# 警告も含めて確認
cargo clippy --all-targets --all-features -- -D warnings 2>&1 | less
```

---

## 📞 サポート

### 困ったときの対処法

1. **ビルドエラー**: エラーメッセージを注意深く読み、型定義や import を確認
2. **テスト失敗**: 差分を確認し、公式実装と比較
3. **実装方針不明**: 公式JavaScriptコードを1行ずつRustに翻訳
4. **詰まった**: `AGENTS.md`のプロジェクト構造を再確認

### 有用なコマンド

```bash
# プロジェクト全体の構造を確認
tree src/compiler/phases -L 3

# 特定のパターンを検索
rg "pattern" src/

# 最近の変更を確認
git log --oneline -10
git show HEAD

# テストカテゴリーを確認
npm run list-categories
```

---

## 📝 作業完了時のチェックリスト

作業完了時には以下を確認してください：

- [ ] `cargo build` が成功
- [ ] `cargo test --test compiler_fixtures` でテスト合格率を確認
- [ ] `cargo fmt` でコードフォーマット
- [ ] `cargo clippy` で警告なし（または対処）
- [ ] `git status` で意図しない変更がないか確認
- [ ] コミットメッセージに成果を明記
- [ ] `git push` でリモートに反映
- [ ] 次の作業者向けに **この指示書を更新**

---

## 🚀 次のフェーズへの展望

**スナップショットテスト100%達成後**:

1. **Runtime Testsの改善** (現在: 1.4%)
   - より多くのvisitor実装
   - reactive systemの完成度向上
   - 目標: 10%+ → 30%+

2. **SSR Testsの改善** (現在: 12.5%)
   - Server-side renderingの完全実装
   - 目標: 30%+ → 50%+

3. **CSS Scoping完成** (現在: 62.1%)
   - 複雑なセレクターの処理
   - 未使用CSS検出の精度向上
   - 目標: 80%+ → 95%+

---

**Good luck! 🎉**

**Remember**: サブエージェントを活用して並列作業を進めることで、効率的に進められます！
