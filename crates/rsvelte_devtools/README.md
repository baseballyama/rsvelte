# rsvelte_devtools

Repository-local benchmark, profiling, compatibility, and reporting binaries
for rsvelte development.

This crate is not published. Production compiler and embedding APIs belong to
`rsvelte_core`; end-user command-line products live in their owning crates.
