[package]
name = "rsvelte_fmt"
version = "0.7.10"
edition = "2024"
rust-version = "1.95"
description = "Fast Svelte + JS/TS/CSS formatter — dispatches .svelte to rsvelte_formatter and other files to oxfmt"
license = "MIT"
publish = false

[lib]
# rlib only — the CLI binary and the language server both link the same
# formatting pipeline from here.
crate-type = ["rlib"]

[[bin]]
name = "rsvelte-fmt"
path = "src/main.rs"

# Benchmark driver invoked by `scripts/bench/run-benchmark.mjs` for the `fmt`
# task. Lives here (not in `rsvelte_devtools`' `benchmark_runner`) because the
# formatter sits in `rsvelte_formatter`, which depends on `rsvelte_core` —
# the reverse dependency would be a cycle.
[[bin]]
name = "fmt_benchmark_runner"
path = "src/bin/fmt_benchmark_runner.rs"

[dependencies]
rsvelte_formatter = { path = "../rsvelte_formatter" }
tailwind_class_order = { path = "../tailwind_class_order" }
oxc_formatter = { git = "https://github.com/oxc-project/oxc", rev = "9a423f2f485b79c2353c49442c0c7f60f900261d" }
oxc_formatter_core = { git = "https://github.com/oxc-project/oxc", rev = "9a423f2f485b79c2353c49442c0c7f60f900261d" }
# Static evaluation of `oxfmt.config.ts` / `.mts` (see src/ts_config.rs) — no
# embedded JS runtime here, so the config is parsed and interpreted directly
# instead of `import()`-ed like oxfmt's own NAPI build does.
oxc_allocator = { git = "https://github.com/oxc-project/oxc", rev = "9a423f2f485b79c2353c49442c0c7f60f900261d" }
oxc_ast = { git = "https://github.com/oxc-project/oxc", rev = "9a423f2f485b79c2353c49442c0c7f60f900261d" }
oxc_parser = { git = "https://github.com/oxc-project/oxc", rev = "9a423f2f485b79c2353c49442c0c7f60f900261d" }
oxc_span = { git = "https://github.com/oxc-project/oxc", rev = "9a423f2f485b79c2353c49442c0c7f60f900261d" }
clap = { version = "4.5", features = ["derive"] }
walkdir = "2.5"
rayon = "1.10"
anyhow = "1.0"
serde_json = "1.0"
ignore = "0.4.25"
mimalloc = "0.1"

[lints]
workspace = true
