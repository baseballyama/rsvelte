//! Generated data tables consumed by rules, kept out of `rules/` proper (a
//! `Rule` implementation per file) so a large data-only module doesn't read
//! like an oversized rule.

pub mod known_css_properties;
