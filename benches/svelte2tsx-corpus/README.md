# svelte2tsx benchmark corpus

Projection-only fixtures with stable benchmark identities live here. They are
kept separate from `benches/corpus` so adding a projection case does not change
the workload of aggregate parser and compiler benchmarks.
