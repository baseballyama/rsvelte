# LSP protocol fixtures

The lightweight protocol cases are declared in
`scripts/compat-lsp/upstream-fixture-manifest.json`. Their inline sources use
URIs below this directory so both language servers see the same tracked
workspace root without generated fixture files.
