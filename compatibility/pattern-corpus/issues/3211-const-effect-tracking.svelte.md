# `3211-const-effect-tracking.svelte`

**Issue:** [#3211](https://github.com/baseballyama/rsvelte/issues/3211)

`$effect.tracking()`, which lowers to a literal rather than to a call — a different arm of the same lowering, so it prices the fix rather than restating it
