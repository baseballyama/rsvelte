# `template-local-shadows-derived-let.svelte`

**Issue:** corpus residue

The same write through `let`, which does not hoist, so the declaration keyword is not the axis. Recorded because an earlier version of this cell asserted that it was: measured against a deliberate name-keyed mis-fix, the keyword claim was false and the write was what discriminated.
