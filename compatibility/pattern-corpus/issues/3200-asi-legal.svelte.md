# `3200-asi-legal.svelte`

**Issue:** [#3200](https://github.com/baseballyama/rsvelte/issues/3200)

The control: the same statements one per line, where ASI applies and both compilers accept. The three files above are the only repros in this directory the **formatter** oracle cannot read — oxfmt needs parseable JS — so they are skipped from the fmt parity set and this is the one that keeps it covered
