<script>
	export let a = 1 // literal default: the value stays the call's last argument
	export let b = 2; /* the `;` is not an axis */
	let d = 3 // the second host reaches the same lowering
	export { d };
</script>

<p>{a}{b}{d}</p>
