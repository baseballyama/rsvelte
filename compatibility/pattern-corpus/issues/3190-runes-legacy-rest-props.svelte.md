# `3190-runes-legacy-rest-props.svelte`

**Issue:** [#3190](https://github.com/baseballyama/rsvelte/issues/3190)

`$$restProps` alone, in an attribute value. Needed because the file above can only ever report `legacy_props_invalid`: an entry listed for one error suppresses everything about that entry
