# `3207-unclosed-await.svelte`

**Issue:** [#3207](https://github.com/baseballyama/rsvelte/issues/3207)

The `{#await}` spelling, whose close is a three-branch state machine rather than the single-branch one `{#each}` uses
