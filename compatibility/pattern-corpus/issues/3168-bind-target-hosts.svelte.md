# `3168-bind-target-hosts.svelte`

**Issue:** [#3168](https://github.com/baseballyama/rsvelte/issues/3168)

The **legal** half of the rule across all four hosts, which is what a single file can carry: each of the three defects is a compile error on one side, so a file reproducing them would be scored `error-parity` and would hide every other line. The contenteditable pair is the direct control for the over-acceptance — the same binding, once on a dynamic element and once on a `<p>`, both with the attribute the fix now demands
