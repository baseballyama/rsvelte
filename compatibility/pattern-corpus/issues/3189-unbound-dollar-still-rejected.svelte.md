# `3189-unbound-dollar-still-rejected.svelte`

**Issue:** [#3189](https://github.com/baseballyama/rsvelte/issues/3189)

The control: a genuinely unresolved `$`, which must still be `global_reference_invalid`. Without it the fix reads as "stop raising the error" rather than "raise it only for an unresolved reference"
