# `2138-legacy-assignment-destructure-rest.svelte`

**Issue:** [#2138](https://github.com/baseballyama/rsvelte/issues/2138)

A legacy destructuring **assignment** with quoted / computed keys and a rest — bracket member reads, an `$.exclude_from_object` key list built like `b.literal(...)`, and no `$$value` IIFE for an identifier right-hand side
