# `3140-meta-in-head.svelte`

**Issue:** [#3140](https://github.com/baseballyama/rsvelte/issues/3140)

A meta element nested in `<svelte:head>` — the container that is itself root-only, so the check has to reject from inside the very node it licenses at the top level
