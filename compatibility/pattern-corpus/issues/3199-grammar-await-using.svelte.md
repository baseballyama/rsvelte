# `3199-grammar-await-using.svelte`

**Issue:** [#3199](https://github.com/baseballyama/rsvelte/issues/3199)

`await using` — a separate declaration kind in OXC's AST, so a check that matched only `Using` would let this one through
