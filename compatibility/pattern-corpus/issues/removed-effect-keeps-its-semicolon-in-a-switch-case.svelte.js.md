# `removed-effect-keeps-its-semicolon-in-a-switch-case.svelte.js`

**Issue:** corpus residue

The `compileModule` half of the row above, plus the unbraced `if` / `else` slot and `$effect.pre` — the second and third statement slots that are not a `body` sequence.
