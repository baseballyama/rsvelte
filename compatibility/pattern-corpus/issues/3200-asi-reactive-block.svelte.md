# `3200-asi-reactive-block.svelte`

**Issue:** [#3200](https://github.com/baseballyama/rsvelte/issues/3200)

The same omission inside a `$:` block body, which is the shape the generated legacy grid found it on and the one semicolon-free real-world source actually reaches
