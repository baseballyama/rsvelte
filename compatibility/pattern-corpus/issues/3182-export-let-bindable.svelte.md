# `3182-export-let-bindable.svelte`

**Issue:** [#3182](https://github.com/baseballyama/rsvelte/issues/3182)

The same precedence through `$bindable`, whose error comes from a different placement check than `$host`'s — one rune cannot show the ordering is general
