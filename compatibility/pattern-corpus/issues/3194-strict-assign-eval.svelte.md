# `3194-strict-assign-eval.svelte`

**Issue:** [#3194](https://github.com/baseballyama/rsvelte/issues/3194)

`eval = 1` — assigning to `eval`, which is a separate rule from binding it and carries a separate message
