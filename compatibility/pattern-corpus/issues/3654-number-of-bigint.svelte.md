# `3654-number-of-bigint.svelte`

**Issue:** [#3654](https://github.com/baseballyama/rsvelte/issues/3654)

`Number(bigint)` follows `ToNumber` while `String`, `Number.isInteger` and `BigInt` retain their distinct bigint rules.
