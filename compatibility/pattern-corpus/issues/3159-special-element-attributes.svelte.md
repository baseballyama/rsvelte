# `3159-special-element-attributes.svelte`

**Issue:** [#3159](https://github.com/baseballyama/rsvelte/issues/3159)

The **legal** half of the attribute rules for the four special hosts, which is the direction a repro can carry: an illegal attribute there is a compile error, so each one would need its own file and would be scored `error-parity` rather than compared. `<svelte:window>` also takes a `bind:`, which is legal beside a handler and must survive the new rejection
