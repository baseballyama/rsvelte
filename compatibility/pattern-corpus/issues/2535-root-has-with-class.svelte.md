# `2535-root-has-with-class.svelte`

**Issue:** [#2535](https://github.com/baseballyama/rsvelte/issues/2535)

The `:root` member of #2535's five families, which its table lists by name with the shape left blank. `:root.x:has(.a)` against a `<div class="a">`: a `:root` compound that also carries a class, and a `:has()` subject on top of it
