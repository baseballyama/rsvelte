# `template-local-shadows-state.svelte`

**Issue:** corpus residue

Negative half of the axis: an outer `$state` is not folded, so the same written local must stay inert. Measured green on all three arms (`main`, the fix, the name-keyed mis-fix) and all four targets — it rejects a fix that keys on shadowing alone.
