<script lang="ts">
  type OwnProps = {
    /** First prop. */
    use?: string;
    /** Second prop. */
    class?: string;
  };

  const between = 1;
  let props: OwnProps = { use: '' };
</script>

{between}
