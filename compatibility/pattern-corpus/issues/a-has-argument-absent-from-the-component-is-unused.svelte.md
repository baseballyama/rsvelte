# `a-has-argument-absent-from-the-component-is-unused.svelte`

**Issue:** css-prune probe

`is_has_argument_unused` bails to `keep` whenever `has_opaque_sibling_boundaries` is set and the subject's structural data is thin — a slot, a render tag, a component or a non-exhaustive `{#await}` — on the reasoning that injected content could supply the match. Upstream matches a `:has()` argument against the component's OWN elements and nothing else, so a class no element here carries is unused however incomplete the data is. Measured before assuming it: on a 4-combinator x 7-opaque-host grid the official compiler reports UNUSED in 28 of 28 cells and keeps NONE of them, while rsvelte kept 13 — the descendant arm was already right and `>`, `+` and `~` were not. The guard now returns unused when no element matches the argument's first compound at all, ahead of every conservative bail. The four controls are the same combinators against a class that IS present, plus `:has(:focus-visible)`, which has no constraint to be absent — a fix that simply deleted the bails reports those too. Measured on the 80-entry warning ratchet: entries with a code divergence 1 -> 0, which takes the whole 78-entry compared population to ZERO code divergences on all four targets. Ablated, this file reports one of the four and its CSS stops matching.
