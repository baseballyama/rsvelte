# `template-local-no-outer-name.svelte`

**Issue:** corpus residue

Negative half: no outer binding of the name exists, so nothing may be grafted. Green on all three arms and all four targets; it rejects a resolver that would attach an initializer to whatever record it finds.
