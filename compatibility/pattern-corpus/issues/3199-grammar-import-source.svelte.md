# `3199-grammar-import-source.svelte`

**Issue:** [#3199](https://github.com/baseballyama/rsvelte/issues/3199)

`import source s from` — the other phase, carried separately because both spellings share one `phase` field and a single file cannot show the field is read rather than the keyword matched
