# `3270-switch-case-bare.svelte`

**Issue:** [#3270](https://github.com/baseballyama/rsvelte/issues/3270)

A `switch` case **without** braces, where the declaration is a sibling statement rather than a block body — the arm a fix written against `BlockStatement` alone would miss
