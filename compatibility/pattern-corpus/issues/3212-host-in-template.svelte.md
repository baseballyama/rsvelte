# `3212-host-in-template.svelte`

**Issue:** [#3212](https://github.com/baseballyama/rsvelte/issues/3212)

`$host()` outside a custom element — a rule that needs no parent at all, kept because it is the cheapest proof the delegation happens rather than the placement helpers being reimplemented
