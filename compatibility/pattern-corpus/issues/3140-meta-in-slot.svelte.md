# `3140-meta-in-slot.svelte`

**Issue:** [#3140](https://github.com/baseballyama/rsvelte/issues/3140)

`<slot>` is the fourth: a legacy element that is neither a component nor a regular element, so it fell outside all three counters for the same reason the other three did
