# `3212-arity-in-template.svelte`

**Issue:** [#3212](https://github.com/baseballyama/rsvelte/issues/3212)

`$state.snapshot()` with no argument. Arity is context-free, so this row separates 'the rune rules run here' from 'the placement helpers work here'
