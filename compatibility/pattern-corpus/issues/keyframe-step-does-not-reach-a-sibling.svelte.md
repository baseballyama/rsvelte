# `keyframe-step-does-not-reach-a-sibling.svelte`

**Issue:** corpus residue

The same step under a rule that DOES match: it scopes that element and not its sibling. This is the discriminating half — an unmatched parent alone is equally explained by a rule keyed on whether the parent is used.
