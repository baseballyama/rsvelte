---
"@rsvelte/compiler": patch
---

Write short esrap fragments with fixed-width copies and decide indentation before rendering hot sequences. Release `rsvelte_esrap` 0.10.26 and update the compiler's exact dependency.
