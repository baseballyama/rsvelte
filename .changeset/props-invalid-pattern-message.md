---
"@rsvelte/compiler": patch
---

Report `props_invalid_pattern` with upstream's wording. The code, start and end already agreed; only the message text was rsvelte's own, which the corpus error gate ratchets separately precisely because `code` is saturated.
