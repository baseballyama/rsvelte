---
'@rsvelte/compiler': patch
---

Accept a `$`-prefixed name bound in the template — an each item or index, a snippet's name or parameter, `{@const}`, `{#await}`'s value or error — which the official compiler allows in both runes and legacy mode
