---
"@rsvelte/compiler": patch
"@rsvelte/vite-plugin-svelte-native": patch
---

`parse()`: a class `implements` clause is the `TSExpressionWithTypeArguments` array acorn-typescript emits, not the boolean `true`
