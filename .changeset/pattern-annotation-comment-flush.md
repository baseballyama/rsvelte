---
"@rsvelte/compiler": patch
---

A comment left behind by an erased TypeScript annotation on a destructuring pattern now lands inside the pattern's brackets, where upstream puts it, instead of ahead of the initializer
