---
"@rsvelte/compiler": patch
---

Count a separator space in the printer's fit measurement, as esrap does. A declaration list whose printed length was exactly 51 measured 50 and stayed on one line — including the expanded `let { a = 1 } = $state()` of a module script.
