---
"@rsvelte/compiler": patch
"@rsvelte/svelte2tsx": patch
"@rsvelte/svelte-check": patch
---

Preserve the source-width padding for attributes and actions on `<svelte:element>` when its `this` expression is a string literal.
